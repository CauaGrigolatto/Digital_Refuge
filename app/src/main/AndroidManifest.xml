<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />


    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitalRefuge">

        <activity
            android:name=".ui.activity.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.NewEntryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.EntryDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.AuthActivity"
            android:exported="true"
            android:theme="@style/Theme.RefugioDigital.NoActionBar">

            <!-- Este filtro diz ao sistema Android: -->
            <intent-filter>
                <!-- "Esta é a atividade principal (MAIN) do aplicativo." -->
                <action android:name="android.intent.action.MAIN" />

                <!-- "Ela deve ser o ponto de entrada quando o usuário clica no ícone (LAUNCHER)." -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="br.edu.ifsp.dmo.digitalrefuge.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>